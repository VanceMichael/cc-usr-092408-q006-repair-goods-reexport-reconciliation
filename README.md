# 跨境贸易事件版本归并

这是跨境贸易事件服务的后端基线，已有 Node.js 健康入口和 SQLite 基础迁移，后续可继续扩展事件接收、审核与查询能力。

## 本地验证

执行测试：

```bash
npm test
```

执行编译或构建检查：

```bash
npm run build
```

所有验证均在单个 Linux 应用环境中完成，不需要浏览器或独立运行的数据库、缓存与消息队列。
